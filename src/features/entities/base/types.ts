export type Attributes = {
  STR: number;
  DEX: number;
  INT: number;
  VIT: number;
  WIS: number;
}

export type Vitals = {
  currentHP: number;
  maxHP: number;
  currentMP: number;
  maxMP: number;
}